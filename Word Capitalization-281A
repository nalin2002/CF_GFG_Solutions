#include <iostream>
#include <string>
using namespace std;

int main()
{
   string inp;
   cin>>inp;
   if((int)inp[0]>=65 && (int)inp[0]<=90)
   cout<<inp<<endl;
   else{
       inp[0]=(char)(((int)inp[0])-32);
       cout<<inp<<endl;
   }
   return 0;
}
